## ShieldAI Environment Variables

# Google API Key (optional - for enhanced semantic analysis)
GOOGLE_API_KEY=your-google-api-key-here

# Database Configuration (automatically handled in production)
DATABASE_PATH=data/shieldai.db

# Server Configuration
MAX_PROMPT_LENGTH=10000
RATE_LIMIT=100
DEBUG=False
PORT=8000

# Detection Thresholds (0.0 to 1.0)
PATTERN_THRESHOLD=0.55
ANOMALY_THRESHOLD=0.60
SEMANTIC_THRESHOLD=0.65
FINAL_BLOCK_THRESHOLD=0.70

# Ensemble Weights (must sum to ~1.0)
W_PATTERN=0.35
W_SEMANTIC=0.45
W_ANOMALY=0.20

# --- Additional recommended settings ---

# Authentication (use a long random string in production)
AUTH_SECRET_KEY=change-me-to-a-long-random-secret
AUTH_TOKEN_TTL_SECONDS=604800

# Optional Anthropic key (platform runs without it)
ANTHROPIC_API_KEY=

# Google OAuth client for "Sign in with Google" (optional)
GOOGLE_OAUTH_CLIENT_ID=

# Model artifact path (used when not running in-memory)
MODEL_PATH=data/attack_classifier.joblib


